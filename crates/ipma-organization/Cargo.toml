[package]
name = "ipma-organization"
version = "0.1.0"
edition.workspace = true
authors = ["oi-io <boss@oi-io.cc>"]
description = "IPMA Organization - org tree, employees and org templates"
license = "GPL-3.0-or-later"

[dependencies]
# Workspace crates
ipma-common = { path = "../ipma-common" }
ipma-models = { path = "../ipma-models" }
ipma-auth = { path = "../ipma-auth" }

# Axum
axum = { workspace = true }

# SQLx
sqlx = { workspace = true }

# Serialization
serde_json = { workspace = true }

# Validation
validator = { workspace = true }

# Date/Time
chrono = { workspace = true }

# UUID
uuid = { workspace = true }

[lints]
workspace = true
