[package]
name = "ipma-models"
version = "0.2.0"
edition.workspace = true
authors = ["oi-io <boss@oi-io.cc>"]
description = "IPMA Domain Models - shared request/response and database row models"
license = "GPL-3.0-or-later"

[dependencies]
# Workspace crates
ipma-common = { path = "../ipma-common" }
ipma-scheduler = { path = "../ipma-scheduler" }

# Serialization
serde = { workspace = true }

# Validation
validator = { workspace = true }

# SQLx
sqlx = { workspace = true }

# Date/Time
chrono = { workspace = true }

# UUID
uuid = { workspace = true }

# JSON
serde_json = { workspace = true }

[lints]
workspace = true
