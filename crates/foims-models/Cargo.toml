[package]
name = "foims-models"
version = "0.2.7"
edition.workspace = true
authors = ["oi-io <boss@oi-io.cc>"]
description = "FOIMS Domain Models - shared request/response and database row models"
license = "GPL-3.0-or-later"

[dependencies]
# Workspace crates
foims-common = { path = "../foims-common" }
foims-scheduler = { path = "../foims-scheduler" }

# Serialization
serde = { workspace = true }

# Validation
validator = { workspace = true }

# SQLx
sqlx = { workspace = true }

# Date/Time
chrono = { workspace = true }

# UUID
uuid = { workspace = true }

# JSON
serde_json = { workspace = true }

[lints]
workspace = true
