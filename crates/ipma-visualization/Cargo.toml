[package]
name = "ipma-visualization"
version = "0.2.1"
edition.workspace = true
authors = ["oi-io <boss@oi-io.cc>"]
description = "IPMA Visualization Module - Layout management and visualization"
license = "GPL-3.0-or-later"

[dependencies]
# Workspace crates
ipma-common = { path = "../ipma-common" }
ipma-auth = { path = "../ipma-auth" }
ipma-models = { path = "../ipma-models" }

# Axum
axum = { workspace = true }

# SQLx PostgreSQL
sqlx = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Validation
validator = { workspace = true }

# Utilities
uuid = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
