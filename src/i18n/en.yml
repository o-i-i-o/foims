# English language resources (backend system logs only)
# API 消息不在此文件：后端只返回 key，由前端翻译

log:
  auth:
    external_user_created: "External auth user created: {{username}} ({{provider}})"
    external_email_conflict: "External auth email conflict, using placeholder: {{username}} -> {{email}}"
    check_revoke_failed: "Failed to check token revocation: {{error}}"
    clear_2fa_code_failed: "Failed to clear 2FA email code: {{error}}"
    jwt_cache_cleanup_stopped: "JWT cache cleanup task received shutdown signal, stopping"
    jwt_secret_from_env: "JWT secret loaded from environment variable"
    jwt_secret_invalid: "JWT secret validation failed: {{detail}}"
    jwt_secret_missing_temp: "JWT secret not configured, temporary secret generated (development only)"
    jwt_secret_weak: "JWT secret complexity is low; mix upper/lowercase letters, digits and special characters"
    revoke_access_token_failed: "Failed to revoke access token: {{error}}"
    revoke_refresh_token_failed: "Failed to revoke refresh token: {{error}}"
    revoke_token_failed: "Failed to revoke token: {{error}}"
    save_reset_token_failed: "Failed to save reset token: {{error}}"
    send_reset_email_failed: "Failed to send reset email: {{error}}"
  backup:
    dir_permission_failed: "Failed to set backup directory permissions for {{path}}: {{error}}"
  cleanup:
    old_backup_delete_failed: "Failed to delete old backup file: {{path}} - {{error}}"
    old_backup_deleted: "Deleted old backup file: {{path}}"
  config:
    file_written: "Configuration written to file"
    save_start: "Saving configuration to: {{path}}"
    saved: "Configuration saved to: {{path}}"
    toml_length: "Configuration TOML serialized, length: {{count}}"
    verify_read: "Verification read succeeded, length: {{count}}"
  crypto:
    backup_create_failed: "Failed to create key backup: {{error}}, does not affect operation"
    backup_integrity_passed: "Backup key integrity check passed (matches main key)"
    backup_key_path: "Backup key path: {{path}}"
    backup_length_invalid: "Backup key length invalid, a new key will be generated"
    backup_mismatch: "Backup key differs from main key"
    backup_mismatch_corruption: "It may also signal data corruption if the main key was restored from backup"
    backup_mismatch_recent: "This may be normal if the main key was updated recently"
    backup_missing: "Key backup file not found"
    backup_only_detected: "Backup key detected but main key missing, restoring from backup"
    backup_read_failed: "Failed to read backup key: {{error}}"
    backup_save_failed: "Failed to save key backup: {{error}}, does not affect operation"
    cipher_init_failed: "Failed to create cipher: {{error}}"
    decrypt_detail: "Key length: {{key_len}} bytes, nonce length: {{nonce_len}} bytes, ciphertext length: {{ciphertext_len}} bytes"
    decrypt_failed_hint: "AES-GCM decryption failed. Possible causes: 1. encrypted data uses an old key; 2. key file modified or deleted while running; 3. key lost after restart/rebuild. Remedies: check /etc/ipma/encryption.key.backup and restore; otherwise reconfigure affected encrypted data (SNMP/SMTP passwords, 2FA secrets)"
    encrypt_failed: "Encryption failed: {{error}}"
    integrity_check_passed: "Encryption key integrity check passed"
    key_backed_up: "Encryption key backed up to: {{path}}"
    key_generated: "Encryption key generated and saved to: {{path}}"
    key_generating: "Encryption key file not found, generating a new key: {{path}}"
    key_read_failed: "Failed to read encryption key file: {{error}}"
    key_resave_failed: "Failed to re-save key after restore: {{error}}"
    key_restored: "Key successfully restored from backup"
    main_key_path: "Main key path: {{path}}"
    restore_from_backup: "Attempting to restore from backup key"
    set_permissions_failed: "Failed to set file permissions {{path}}: {{error}}"
  db:
    health_check_failed: "DB pool health check failed: {{error}} (active: {{active}}, idle: {{idle}}, waiting: {{waiting}}, size: {{size}})"
    health_check_stopped: "DB pool health check task received shutdown signal, stopping"
    pool_closed: "Database connection pool closed"
    pool_exhausted: "Pool fully exhausted! All {{total}} connections in use, waiting: {{waiting}}"
    pool_near_exhausted: "Pool nearly exhausted, possible leak! Active: {{active}}/{{total}}, utilization: {{utilization}}%, threshold: {{threshold}}%"
    pool_status: "Pool status - active: {{active}}, idle: {{idle}}, waiting: {{waiting}}, size: {{size}}"
    system_time_abnormal: "System clock abnormal: {{error}}, using 0 as timestamp"
  device:
    lldp:
      record_write_failed: "LLDP record write failed (local_port={{port}}): {{error}}"
    mac:
      query_failed: "Failed to query MAC table: {{error}}"
      record_write_failed: "MAC record write failed (ip={{ip}}, mac={{mac}}): {{error}}"
    snmp:
      auth_built: "Authentication built"
      if_name_walk_failed: "SNMP walk for interface name map failed"
      private_ip_allowed: "Target IP {{ip}} is private, connection allowed"
      test_connecting: "Connecting: {{addr}} (timeout: {{timeout}}s)"
      test_params: "Test params: ip={{ip}}, port={{port}}, version={{version}}, community={{community}}, username={{username}}"
      test_request: "Test connection: device_id={{device_id}}, ip={{ip}}, version={{version}}"
      ula_ip_allowed: "Target IPv6 {{ip}} is unique local, connection allowed"
      vlan_walk_failed: "SNMP walk for VLAN map failed"
  error:
    database_detail: "Database error detail: {{detail}}"
    internal_detail: "Internal error detail: {{detail}}"
  fail2ban:
    auth_log_open_failed: "Failed to open auth log file {{path}}: {{error}}"
    auth_log_write_failed: "Failed to write auth log: {{error}}"
    ip_banned: "IP {{ip}} failed login {{count}} times, banned for {{seconds}} seconds"
  i18n_config_error: "i18n logging config error, falling back to English-only logs: {{reason}}"
  import:
    cabinet_network_query_failed: "Failed to query cabinet network: {{error}}"
    existing_ip_query_failed: "Failed to query existing IP: {{error}}"
    file_failed: "Failed to import {{file}}: {{error}}"
    network_lookup_failed: "Failed to look up network: {{error}}"
    room_network_delete_failed: "Failed to delete room network associations: {{error}}"
    room_network_insert_failed: "Failed to insert room network association: {{error}}"
    room_network_query_failed: "Failed to query room network: {{error}}"
    switch_network_query_failed: "Failed to query switch network: {{error}}"
    switch_position_create_failed: "Failed to create switch position record: {{error}}"
    tx_commit_failed: "Failed to commit transaction for {{file}}: {{error}}"
    tx_rollback_failed: "Failed to rollback transaction for {{file}}: {{error}}"
  init:
    admin_create_failed: "Failed to create the admin user"
    creating_admin: "Creating admin user"
    db:
      backup_created: "Database backup created"
      backup_done_dropping: "Backup completed, dropping the old database"
      cleared: "Database cleared via API"
      clearing: "Clearing the database via API"
      connections_terminated: "All connections to database {{name}} terminated"
      created: "Database {{name}} created"
      database_created: "Database created successfully"
      dropped: "Database {{name}} dropped"
      empty_db_recreating: "Database exists but is empty, dropping and recreating"
      exists_skip_create: "Database {{name}} already exists, skipping creation"
      grant_postgres_failed: "Failed to grant postgres privileges"
      grant_public_failed: "Failed to grant public privileges"
      imported: "Database imported successfully"
      imported_from_file: "Database imported from file successfully"
      missing_creating: "Database {{name}} does not exist, creating it"
      not_empty_backing_up: "Database is not empty, backing up"
      public_schema_created: "public schema created successfully"
      schema_created: "Database schema initialized successfully"
      schema_exists: "Database schema already exists, skipping initialization"
      schema_missing_creating: "public schema does not exist, creating it"
      temp_file_remove_failed: "Failed to remove the SQL temporary file"
    index_create_failed: "Index creation failed (may already exist)"
    psql_check_failed: "Failed to check psql installation"
    restart_requested: "Restart request received, preparing to restart"
    system_initialized: "System initialized, admin user created, init mode disabled"
    system_time_warning: "System time calculation warning"
    trigger_create_failed: "updated_at trigger creation failed (may already exist)"
    trigger_drop_failed: "Failed to drop the old trigger"
    verification:
      banner_title: "System Initialization Verification Code"
      code: "Verification code: {{code}}"
      expiry: "Valid for {{minutes}} minutes"
      hint: "Enter this code on the initialization page to complete system initialization"
    view_drop_failed: "Failed to drop the old view"
    view_grant_failed: "Failed to grant privileges on the view"
  init_warning: "Logging init warning: {{detail}}"
  ip:
    batch_record_failed: "Batch create: record {{index}} processing failed: {{error}}"
    invalid_cidr: "Invalid CIDR format, cannot find available IPs: '{{cidr}}'"
    mac_change_notification_failed: "Failed to send MAC change notification: IP={{ip}}, error: {{error}}"
    mac_change_notification_sent: "MAC change notification sent: IP={{ip}}"
    mac_changed: "MAC address change detected: interface={{interface}}, IP={{ip}}, old MAC={{old_mac}}, new MAC={{new_mac}}"
    mac_conflict: "MAC conflict: {{mac}} already used by another device's IP {{ip}}, skipping update"
    mac_observation_conflict: "MAC observation conflict: interface {{interface}} has both {{existing_mac}} and {{new_mac}}, skipping {{ip}}"
    mac_sync_failed: "MAC sync failed: {{error}}"
    mac_written: "MAC address written: interface={{interface}}, MAC={{mac}}"
  login:
    2fa_success: "User {{username}} logged in with 2FA"
    record_failed: "Failed to record login log: {{error}}"
    success: "User {{username}} logged in successfully"
    token_refreshed: "User {{username}} token refreshed"
  mac_change:
    email_sent: "MAC change email sent: workstation={{workstation}}"
    email_skipped: "MAC change email skipped: workstation={{workstation}}, reason: {{reason}}"
    notification_created: "MAC change notification created: workstation={{workstation}}, IP={{ip}}"
  network:
    check_ipv4_duplicate_failed: "Database query failed while checking IPv4 network duplication: {{error}}"
    check_ipv6_duplicate_failed: "Database query failed while checking IPv6 network duplication: {{error}}"
    created: "Network {{name}} created, ID: {{id}}"
    deleted: "Network deleted, ID: {{id}}"
    updated: "Network {{name}} updated, ID: {{id}}"
  operation:
    record_failed: "Failed to record operation log: {{error}}"
  output_to: "Log output to: {{path}}"
  rate_limit:
    blocked: "Request blocked by rate limit: method={{method}}, path={{path}}, ip={{ip}}, user_id={{user_id}}, strict={{is_strict}}, email={{is_email}}, retry_after={{retry_after}}s"
    cleanup_stopped: "Rate limit cleanup task received shutdown signal, stopping"
    triggered: "Rate limit triggered: key={{key}}, count={{count}}, weighted={{weighted}}, limit={{limit}}, window={{window}}s, retry_after={{retry_after}}s"
  shutdown:
    requested: "Shutdown signal received, starting graceful shutdown"
    sighup_ignored: "SIGHUP received, ignored (use the API to reload configuration)"
    sighup_register_failed: "Failed to register SIGHUP handler: {{error}}"
    sigint_received: "SIGINT received"
    sigint_register_failed: "Failed to register SIGINT handler: {{error}}"
    sigterm_received: "SIGTERM received"
    sigterm_register_failed: "Failed to register SIGTERM handler: {{error}}"
  forwarding:
    send_failed: "Log forwarding failed: {{error}}"
    test_sent: "Log forwarding test message sent"
  smtp:
    device_notify_failed: "Device IP notification query failed: {{error}}"
    device_notify_skipped: "Skipping device IP email (no workstation match / manager email / SMTP not configured)"
    incomplete: "SMTP configuration incomplete, required fields missing"
    insecure_connection: "Sending email over unencrypted SMTP, credentials may be transmitted in plaintext (host: {{host}})"
    no_recipients_configured: "No email recipients configured, skipping MAC change email"
    not_configured: "SMTP configuration not set"
    not_configured_password: "SMTP configuration not set, please enter the password"
    query_failed: "Failed to query SMTP configuration: {{error}}"
    recipients_empty: "Email recipient list is empty, skipping MAC change email"
    recipients_query_failed: "Failed to query email recipient configuration: {{error}}"
  system:
    daemon_reload_failed: "daemon-reload failed: {{error}}"
    db_check_failed: "Database connection check failed: {{error}}"
    disable_init_requested: "Disable init mode request received"
    init_disabled_restarting: "Init mode disabled, config saved, triggering service restart"
    restart_requested: "Restart application request received"
    restart_script_spawn_failed: "Failed to spawn restart script: {{error}}"
    restart_triggered: "Service restart triggered (as service: {{as_service}})"
    service_enable_failed: "Failed to enable service: {{error}}"
    service_state: "Current service state: active={{active}}"
    standalone_restart: "Not running as a service, restarting standalone process"
    systemctl_exit_fallback: "systemctl did not terminate the process, restarting via exit"
    systemctl_restart: "Running systemctl restart {{service}}"
  task:
    backup_completed: "Backup completed: {{path}} ({{size_mb}} MB)"
    completed: "Task {{name}} completed: {{result}}"
    failed: "Task {{name}} failed: {{error}}"
    lock_release_failed: "Failed to release task lock: {{error}}"
    log_cleanup_completed: "Cleaned {{count}} log records older than {{days}} days"
    log_record_failed: "Failed to record task log: {{error}}"
    next_run_calc_failed: "Failed to calculate next run time: {{error}}"
    next_run_calc_task_failed: "Next run time calculation task failed: {{error}}"
    next_run_update_failed: "Failed to update next run time of task {{name}}: {{error}}"
    running: "Running scheduled task {{name}} ({{task_type}})"
    scheduler_started: "Cron scheduler started successfully"
    shutdown_completed: "Scheduler shut down"
    shutdown_failed: "Failed to shut down scheduler: {{error}}"
    sync_failed: "Failed to sync user tasks: {{error}}"
    token_cleanup_completed: "Cleaned {{count}} expired tokens"
    token_usage_cleanup_completed: "Cleaned {{count}} token_usage records older than {{days}} days"
    update_result_failed: "Failed to update scheduled task result: {{error}}"
  token:
    revoked_cleaned: "Cleaned {{count}} expired revoked tokens"
    usage_cleaned: "Cleaned {{count}} token usage records older than {{days}} days"
  user:
    password_history_write_failed: "Failed to write password history: {{error}}"
    password_history_prune_failed: "Failed to prune password history: {{error}}"
    created: "User {{username}} created (ID: {{id}})"
    deleted: "User deleted (ID: {{id}})"
    invalidate_tokens_failed: "Failed to update tokens_invalidated_at: {{error}}"
    updated: "User updated (ID: {{id}})"
  workstation:
    not_found: "Workstation ID not found: {{id}}"
    query_name_failed: "Failed to query workstation name: {{error}}"
  ldap:
    query_failed: "Failed to read LDAP config: {{error}}"
  sso:
    login_redirect: "SSO login redirecting to authorization endpoint: {{issuer}}"
    provider_error: "Identity provider returned an error: {{error}}"
    query_failed: "Failed to read SSO config: {{error}}"

system:
  config_load_failed: "Failed to load configuration file: {{error}}"
  config_loaded: "Configuration loaded successfully"
  db_health_check_started: "Database connection pool health check task started (every {{interval}}s)"
  db_pool_close_timeout: "Database connection pool close timed out: {{error}}"
  db_pool_create_failed: "Failed to create database connection pool: {{error}}"
  db_pool_created: "Database connection pool created: max={{max}}, min={{min}}, acquire_timeout={{acquire_timeout}}s, idle_timeout={{idle_timeout}}s, max_lifetime={{max_lifetime}}s"
  fail2ban_cleanup_started: "Application fail2ban cleanup task started"
  force_exit: "Second interrupt signal received, forcing exit!"
  force_exit_register_failed: "Failed to register force-exit signal handler: {{error}}"
  graceful_shutdown_timeout: "Server graceful shutdown timed out: {{error}}"
  init_mode_disabled: "Initialization mode disabled: full application routes available"
  init_mode_enabled: "Initialization mode enabled: only initialization routes available"
  key_integrity_check_failed: "Encryption key integrity check failed: {{error}}"
  listening_uds: "Listening on: UDS ({{path}})"
  rate_limit_email: "Email limit: {{limit}}/{{window}}s"
  rate_limit_enabled: "Rate limit middleware enabled"
  rate_limit_ip: "IP limit: {{limit}}/{{window}}s"
  rate_limit_login: "Login limit: {{limit}}/{{window}}s"
  rate_limit_user: "User limit: {{limit}}/{{window}}s"
  ready: "System startup complete, waiting for requests..."
  register_backup_job_failed: "Failed to register backup scheduled job: {{error}}"
  register_token_cleanup_job_failed: "Failed to register token cleanup scheduled job: {{error}}"
  register_usage_cleanup_job_failed: "Failed to register token usage cleanup scheduled job: {{error}}"
  scheduler_create_failed: "Failed to create scheduler: {{error}}"
  scheduler_shutdown_timeout: "Scheduler shutdown timed out: {{error}}"
  scheduler_start_failed: "Failed to start scheduler: {{error}}"
  scheduler_started: "Scheduler started successfully"
  server_run_error: "Server run error: {{error}}"
  shutdown_background_signaled: "Shutdown signal sent to background tasks"
  shutdown_complete: "System shut down gracefully"
  shutdown_connections_closed: "Server stopped accepting new connections"
  shutdown_server_done: "All server tasks finished"
  shutdown_step_background: "3. Shutting down background tasks..."
  shutdown_step_connections: "1. Stopping new connections and waiting for requests to complete..."
  shutdown_step_db: "5. Shutting down the database connection pool..."
  shutdown_step_scheduler: "4. Shutting down the scheduler..."
  shutdown_step_server: "2. Waiting for server task to finish..."
  start: "Starting IPMA server..."
  start_time_initialized: "System startup time initialized"
  static_serve_mode: "Static file serving: {{mode}}"
  uds_in_use: "UDS socket {{path}} is already used by another IPMA process, refusing to start to avoid duplicate instances"
  uds_server_started: "UDS server started (h2c): {{path}}"
  uds_stale_cleaned: "Stale socket file detected (no listener), cleaned up: {{path}}"
  web_dir_created: "Created web directory: {{path}}"